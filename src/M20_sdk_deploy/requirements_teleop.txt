# Python deps for the standalone VR / arm teleop nodes.
#   pip3 install -r src/M20_sdk_deploy/requirements_teleop.txt
#
# scipy must be compatible with the installed numpy major version. On a stock
# ROS2 Humble Ubuntu 22.04 image with numpy 2.x, upgrade scipy; with the
# recommended numpy<2, the distro scipy also works.
scipy>=1.12
websockets>=12
PyYAML
